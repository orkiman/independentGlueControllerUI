<UserControl x:Class="GlueControllerUI.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <StackPanel Margin="20" MaxWidth="500">
        <TextBlock Text="Connection" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,20"/>
        
        <materialDesign:Card Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Serial Port" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,10"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ComboBox Grid.Column="0" 
                              ItemsSource="{Binding AvailablePorts}" 
                              SelectedItem="{Binding SelectedPort}"
                              materialDesign:HintAssist.Hint="Select COM Port"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              Margin="0,0,10,0"/>
                    
                    <Button Grid.Column="1" 
                            Command="{Binding RefreshPortsCommand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="Refresh ports">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>
                </Grid>
                
                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <Button Content="Connect" 
                            Command="{Binding ConnectCommand}"
                            IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="0,0,10,0"/>
                    
                    <Button Content="Disconnect" 
                            Command="{Binding DisconnectCommand}"
                            IsEnabled="{Binding IsConnected}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
        
        <materialDesign:Card Padding="20">
            <StackPanel>
                <TextBlock Text="Status" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,10"/>
                
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="12" Height="12" 
                             Fill="{Binding IsConnected, Converter={StaticResource BoolToColorConverter}}"
                             Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding ConnectionStatus}" 
                               Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </StackPanel>
</UserControl>
